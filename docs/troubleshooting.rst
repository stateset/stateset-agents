Troubleshooting
===============

If something does not start:

- Check `/health` and `/healthz`
- Confirm `INFERENCE_BACKEND_URL` points at a reachable backend
- For local dev, set `API_REQUIRE_AUTH=false`

